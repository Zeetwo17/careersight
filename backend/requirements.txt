# ── Core API ──────────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.7
pydantic>=2.5
python-dotenv>=1.0

# ── Data / ML ─────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
scipy>=1.10
scikit-learn>=1.3
lightgbm>=4.0
shap>=0.44
joblib>=1.3

# ── PDF parsing ───────────────────────────────────────────────────────────────
pdfplumber>=0.10

# ── LLM (OpenRouter via OpenAI-compatible client) ────────────────────────────
openai>=1.0

# ── Calibration, conformal risk, fairness, causal, federated, distillation ───
netcal>=1.4
mapie>=1.4
fairlearn>=0.10
causal-learn>=0.1
rapidfuzz>=3.0
econml>=0.16
dowhy>=0.11

# ── DeepHit survival (optional — app degrades gracefully to interpolated curves)
# Uncomment only on a paid Render tier (needs >1 GB RAM):
# torch>=2.0
# torchtuples>=0.2
# pycox>=0.2
